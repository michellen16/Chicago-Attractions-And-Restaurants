<?xml version="1.0" encoding="utf-8"?>
<!-- Michelle Nguyen -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.uic.cs478.s2026.project3app2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="36">

        <!-- ─────────────────────────────────────────── -->
        <!-- Attractions Activity                        -->
        <!-- Directly receives the implicit intent       -->
        <!-- ─────────────────────────────────────────── -->
        <activity
            android:name=".AttractionsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape">

            <!-- This intent-filter makes the Activity directly -->
            <!-- respond to A1's implicit startActivity() call  -->
            <intent-filter>
                <action android:name="edu.uic.cs478.s2026.project3app1.action.SHOW_ATTRACTIONS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <!-- ─────────────────────────────────────────── -->
        <!-- Restaurants Activity                        -->
        <!-- Directly receives the implicit intent       -->
        <!-- ─────────────────────────────────────────── -->
        <activity
            android:name=".RestaurantsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape">

            <!-- This intent-filter makes the Activity directly -->
            <!-- respond to A1's implicit startActivity() call  -->
            <intent-filter>
                <action android:name="edu.uic.cs478.s2026.project3app1.action.SHOW_RESTAURANTS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

    </application>

</manifest>
